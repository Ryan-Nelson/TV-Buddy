@model TV_Buddy.Models.TVSInRooms

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TVSInRooms</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TVSInRoomsId" />
            <div class="form-group">
                <label asp-for="TVSId" class="control-label"></label>
                <select asp-for="TVSId" class="form-control" asp-items="ViewBag.TVSId"></select>
                <span asp-validation-for="TVSId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RoomsId" class="control-label"></label>
                <select asp-for="RoomsId" class="form-control" asp-items="ViewBag.RoomsId"></select>
                <span asp-validation-for="RoomsId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
